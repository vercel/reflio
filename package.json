{
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev -w reflio",
    "build": "npm run build -w reflio",
    "start": "npm run start -w reflio",
    "lint": "npm run lint -w reflio"
  },
  "devDependencies": {
    "eslint": "^8.19.0",
    "eslint-config-custom": "*",
    "prettier": "^2.7.1",
    "prettier-plugin-tailwindcss": "^0.1.12",
    "turbo": "latest"
  }
}
